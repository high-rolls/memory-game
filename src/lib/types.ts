export type CardData = {
  id: number;
  emoji: string;
  isFaceUp: boolean;
  isMatched: boolean;
  isPowerCard: boolean;
  timesSeen: number;
};

export type WindowSize = {
  width: number;
  height: number;
};

